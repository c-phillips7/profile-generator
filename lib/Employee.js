class Employee {
    constructor(name, id, email) {
        this.name = name;
        this.id = id;
        this.email = email
    }

    // helper functions to be a part of the class
        // getName
        // getId
        // getEmail
        // getRole (ie employee, engineer, intern etc)
}